{{data-grid content=largeList
            pageSize=10
            actionList=actionList
            bottomPaginator="page-picker-paginator"
            topPaginator="page-picker-paginator"
            filterEnabled=true
            properties=properties}}
